# Python packages required on the Ansible control machine (CI runner / local)
# Install with: pip install -r pip-requirements.txt

kubernetes>=28.1.0
openshift>=0.13.2
PyYAML>=6.0
azure-identity>=1.15.0
azure-mgmt-containerservice>=28.0.0
